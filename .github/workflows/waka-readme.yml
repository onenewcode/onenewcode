name: Waka Readme

on:
  # for manual workflow trigger
  workflow_dispatch:
  schedule:
    # runs at 12 AM UTC (5:30 AM IST)
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - uses: athul/waka-readme@master # this action name
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          REPOSITORY: onenewcode/mystudy
          TIME_RANGE: all_time # 设置时间范围
          LANG_COUNT: 20 #显示语言数量
          SHOW_TIME: true # 	显示每种语言所花费的时间
          SHOW_MASKED_TIME: true # 添加总编码时间，包括未分类的语言（覆盖：SHOW_TOTAL)
          
